#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(int argc, string argv[])
{
    if (argc == 2)
    {
        // Devemos utilizar [1] para definir o argumento, não o nome do programa.
        //printf("hello, %s\n", argv[1]);
        for (int i = 0; n = strlen(argv[i]); i < n; i++)
            {
                printf("%c", argv[1][i]);
            }
    }
}